pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Hello world"
                echo "Add Something else"
                whoami
            }
        }
    }
}
